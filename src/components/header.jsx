import styled from 'styled-components';

const Header = styled.div`
  padding: 20px;
  background: black;
  color: white;
  display: flex;
  flex-direction: row;
  width: 100vw;
`;

export default Header;
